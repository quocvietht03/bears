!(function($){
	"use strict";

	jQuery(document).ready(function($) {

	});

	jQuery(window).on('resize', function() {
    BearsSubmenuAuto();
	});

	jQuery(window).on('scroll', function() {

	});

})(jQuery);
